# Server
NODE_ENV=development
PORT=4000
API_PREFIX=/api/v1

# Database (PostgreSQL recommended for production)
# For Supabase, use Transaction Pooler (port 6543) with pgbouncer=true
DATABASE_URL="postgresql://postgres:password@localhost:5432/internflow?schema=public"
# For Supabase migrations, use Direct Connection (port 5432)
DIRECT_URL="postgresql://postgres:password@localhost:5432/internflow?schema=public"

# JWT
JWT_ACCESS_SECRET=change-me-to-a-long-random-access-secret
JWT_REFRESH_SECRET=change-me-to-a-long-random-refresh-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS (comma-separated origins)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# File uploads
UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=5
