[build]
  command = "npm run build"
  publish = "dist/client"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
find dist/client -name "*.html" -o -name "*.js" | head -10  force = false

[build.environment]
  NODE_VERSION = "22"
